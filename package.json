{
  "name": "sparks",
  "scripts": {
    "prebuild": "eslint lib/**/*.js build.js",
    "build": "node build.js",
    "start": "http-server build",
    "deploy-staging": "NODE_ENV=staging npm run build && surge build staging.sparks-inc.org",
    "deploy-production": "NODE_ENV=production npm run build && surge build"
  },
  "devDependencies": {
    "css-patterns": "^0.2.0",
    "eslint": "^0.21.2",
    "http-server": "^0.8.0",
    "jade": "^1.9.2",
    "metalsmith": "^1.7.0",
    "metalsmith-autoprefixer": "^1.0.2",
    "metalsmith-fingerprint": "callum/metalsmith-fingerprint",
    "metalsmith-jade": "^2.0.0",
    "metalsmith-sass": "callum/metalsmith-sass",
    "metalsmith-templates": "^0.7.0",
    "node-sass": "^3.1.2",
    "surge": "^0.12.0",
    "xtend": "^4.0.0"
  }
}
